# TSUDOWA モデレーション運用手順

この手順は、Apple App Review Guideline 1.2およびGoogle Playのユーザー生成コンテンツ要件に沿って、通報へ継続的かつ迅速に対応するための内部運用資料です。利用者のパスワード、サービスロールキー、アクセストークンはこの文書やGitへ記録しません。

## 受付経路

- アプリ内のメッセージ・参加者通報
- アプリ内の安全センター
- 公開窓口 `support@tsudowa.app`
- 児童の安全、違法コンテンツ、知的財産権に関する公開窓口

## 確認頻度と初動目標

- 生命・身体の危険、児童の性的搾取、具体的な脅迫: 確認後直ちに対応し、必要に応じて警察その他の関係機関へ連絡する。
- 性的搾取、個人情報の無断公開、重大な嫌がらせ、詐欺: 原則24時間以内に初動する。
- スパム、一般的な規約違反、知的財産権侵害: 原則72時間以内に初動する。
- 未処理通報は毎日確認する。長期不在時は代替確認者を設定するか、新規投稿機能を一時停止する。

## 権限

- 通常アカウントから通報内容を閲覧・変更できない。
- モデレーター権限はSupabase Authのサーバー管理 `app_metadata.role` が `moderator` または `admin` のアカウントだけに付与する。
- モデレーター用アカウントは個人利用アカウントと分離し、多要素認証を有効にする。
- 権限が不要になった場合は直ちに削除する。

## 対応手順

1. Supabaseの `safety_reports` で `received` と `reviewing` を確認し、対象イベント、メッセージ、利用者、理由を照合する。
2. 調査開始時に担当者と状態を記録する。必要以上の個人情報を閲覧・複製しない。
3. 明白な違反コンテンツは `moderation_state` を `hidden` または `removed` に変更する。アプリ利用者には以後返却されない。
4. 写真を削除する場合は、メッセージを非表示化したうえでStorageの `chat-media` から該当オブジェクトも削除する。既発行の署名URLは最長1時間で失効する。
5. 重大または反復する違反は、Supabase Authenticationで対象アカウントを停止する。イベント主催者権限だけで安全対応を完結させない。
6. `resolution_note`、`assigned_to`、`resolved_at` と最終状態を記録する。通報者へ回答できる場合は、プライバシーを侵害しない範囲で受付・対応結果を案内する。
7. 判断に異議がある利用者から連絡があった場合は、当初と異なる観点で再確認する。

## 投稿前フィルター

- アプリ側で送信前に禁止表現を確認する。
- データベースのトリガーでも同じ分類の表現を拒否し、改変クライアントによる回避を防ぐ。
- フィルターだけに依存せず、通報、ブロック、非表示、アカウント停止を組み合わせる。
- 誤検知や新しい回避表現を確認した場合は、利用者の安全と正当な表現の両方を考慮してルールを更新する。

## 証拠と保持

- 違反調査に必要な情報だけを保持し、不要になった時点で削除または匿名化する。
- 児童性的虐待コンテンツなど、所持・複製自体に法的リスクがある情報は不用意にダウンロードしない。
- 法令、捜査機関からの適法な要請、緊急の生命・身体保護が関係する場合は、通常の削除より証拠保全を優先できる。
