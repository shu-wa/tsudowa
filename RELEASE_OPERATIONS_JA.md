# 公開前の運用確認・障害対応

2026-09-11。既存の `MODERATION_OPERATIONS_JA.md` と `PUSH_NOTIFICATIONS_OPERATIONS_JA.md` を補完する。手順の作成は完了、アカウント設定変更・復元試験・実メール試験は未実施。

## 本番の認証メール

- Supabase DashboardのAuthenticationで本番のEmail/SMTP、URL設定、送信上限を確認する。リポジトリの `supabase/config.toml` はローカル設定であり、本番が同じ値だとは判断しない。確認のためにローカル設定を本番へ一括pushしない。
- デフォルトSMTPは公開サービス用ではなく、外部ユーザーへの送信が制限される。カスタムSMTPの有効化、送信者ドメイン認証、本番送信枠を確認する。[Supabase公式](https://supabase.com/docs/guides/auth/auth-smtp)
- アプリが指定するリダイレクトは `tsudowa://onboarding` と `tsudowa://reset-password`。本番の許可リスト・テンプレートがこれを妨げていないか確認する。リダイレクト先を無制限のワイルドカードで許可しない。
- 運営チーム以外の専用受信先で新規登録→確認→再ログイン、再設定→旧パスワード拒否→新パスワード成功を確認する。期限切れ/使用済みリンクでもデータが壊れないことを確認する。
- 記録するものは日時、ビルド、宛先のドメイン、到着/迷惑メール判定、結果だけ。パスワード・OTP・リンク中のトークンは記録しない。
- サポートアドレスでの受信成功だけでは、認証メールの到達やサポート返信の送信者設定を確認したことにはならない。

## バックアップと復元

本番データをここで新たにダウンロード・複製・復元しない。まず保存先、アクセス権、費用、保持期間と削除対応を確定する。

1. 現在のSupabaseプランとDatabase Backupsで、最終成功時刻・保持期間・復元可能な時点を確認する。無料プランの自動バックアップを前提にしない。
2. DBとStorage画像を別に計画する。DBバックアップにはStorageの画像本体が含まれない。[Supabase公式](https://supabase.com/docs/guides/platform/backups)
3. `chat-media` / `app-media` の非公開属性、オブジェクト、所有者メタデータとDB参照の対応を復元できる方式を選ぶ。暗号化した非公開保存先を使い、Git・公開ディレクトリへ置かない。
4. 許容データ損失時間（RPO）、復旧目標時間（RTO）、担当・代替担当、保存頻度と保持期間を運営者が決める。未承認の有料プランや保存先を作成しない。
5. 隔離環境にテスト用DB・写真を作り、削除→復元→参照・RLS・写真閲覧を確認する。本番のRestoreや `db reset --linked` を試験として実行しない。
6. 復元先では通知dispatch/cronと外部メールを止め、本物のユーザーへ通知しない。テスト後はコピーの保持方針に沿って削除する。
7. アカウント削除後に古いバックアップを戻す場合は、削除要求を再適用して削除済み個人情報を復活させない。

## 毎日の確認

| 対象 | 確認すること | 対応 |
| --- | --- | --- |
| 認証/DB/Functions | 停止、5xx増加、登録失敗、データ表示不能 | 影響ビルド・発生時刻を特定。秘密や本文をログへ追加しない |
| 通知 | 定期job有効、期限を過ぎた未処理キュー、失敗回数、実機到達 | 処理済みと受信済みを区別。ticket成功だけで完了扱いしない |
| 通報 | 未処理・重大事案 | モデレーション手順の毎日確認と初動目標を守る |
| バックアップ | 最終成功と復元可能範囲 | 実行失敗を放置しない。取得しただけで復元可能と断定しない |
| 利用量 | DB/Storage/転送/メール/Maps等 | 使用量と予算通知を確認。通知は必ずしも課金の上限ではない |
| サポート | 問い合わせと削除請求、返信可能性 | 本人確認を行い、パスワードを要求しない |

DB集計用の読取専用SQLは `supabase/ops/release-health.sql`。通知本文やトークンを出力しない。SQLを追加しただけでは監視や通知の自動化は開始されない。定期監視の実装は、通知先・頻度・費用を決めてから行う。

## 障害が発生した場合

1. 発生時刻・対象OS/ビルド・症状・影響範囲を記録し、ログイン不能/データ損失/漏洩は優先対応する。
2. 無関係なマイグレーション、全ユーザーの削除、鍵の無計画な交換、失敗操作の連打をしない。
3. 原因がアプリ更新なら配布を止め、最後に確認できたビルドとDB互換性を確認して修正版を用意する。古いバイナリに戻すだけでDBも戻ったとは考えない。
4. 安全上必要なアクセス制限・停止の範囲を決める。証拠や個人情報を一般公開せず、必要な窓口へ適切に連絡する。
5. 修正後に専用アカウントで再現手順・再ログイン・権限を検証し、必要なら利用者へ状況を案内する。

## 残る運営判断

- 専用審査メール/アカウント、運用担当と不在時の代替者。
- バックアップ保存先・保持期間・RPO/RTO・予算。
- 本番SMTP、公開サイト本文、ストア申告の実際の保存状況。
- Android新ビルドとiOSを含む実機試験結果。

これらが未確認のまま、公開前対策をすべて完了したとは記録しない。
